---
import BaseLayout from '../layouts/BaseLayout.astro';
import { defaultLocale } from '../lib/i18n';
const lang = defaultLocale;
const sections = [
  {
    title: 'Article 1 – Objet',
    content:
      'Les présentes conditions générales encadrent l’utilisation du service Tonti, plateforme de financement collaboratif conforme à la loi 15-18.',
  },
  {
    title: 'Article 2 – Inscription',
    content:
      'La création d’un compte nécessite une vérification d’identité (KYC) lors de la connexion à venir. Les informations fournies doivent être exactes et mises à jour.',
  },
  {
    title: 'Article 3 – Contributions',
    content:
      'Aucun paiement réel n’est accepté dans cette version. Les montants simulés servent à tester l’interface et le reporting réglementaire.',
  },
  {
    title: 'Article 4 – Clôture',
    content:
      'Tonti se réserve le droit de clôturer toute cagnotte non conforme aux exigences légales marocaines.',
  },
];
---
<BaseLayout lang={lang} title="Conditions générales – Tonti" description="Conditions générales d'utilisation de Tonti.">
  <section class="py-16">
    <div class="mx-auto w-full max-w-4xl px-4">
      <h1 class="mb-8 text-3xl font-semibold text-white">Conditions générales d'utilisation</h1>
      <div class="space-y-6 text-sm text-white/70">
        {sections.map((section) => (
          <section key={section.title} class="space-y-2">
            <h2 class="text-lg font-semibold text-white">{section.title}</h2>
            <p>{section.content}</p>
          </section>
        ))}
        <p class="text-xs text-white/50">
          Version statique mise à jour le 4 avril 2024. Les mentions légales complètes sont accessibles sur demande.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
