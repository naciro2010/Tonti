---
import BaseLayout from '../layouts/BaseLayout.astro';
import { defaultLocale } from '../lib/i18n';
const lang = defaultLocale;
const faqs = [
  {
    question: 'Tonti est-elle autorisée au Maroc ?',
    answer:
      'Oui. L’architecture respecte les exigences de la loi 15-18 et se contente d’un front statique en attendant l’obtention définitive de l’agrément.',
  },
  {
    question: 'Comment sont sécurisées les contributions ?',
    answer:
      'Les paiements sont actuellement simulés. Aucun numéro de carte ou donnée sensible n’est stocké. Lors du passage en production, une API de paiement agréée sera connectée.',
  },
  {
    question: 'Puis-je gérer plusieurs cagnottes ?',
    answer:
      'Oui, l’interface de création multi-étapes permet d’enregistrer plusieurs brouillons sur le même appareil.',
  },
];
---
<BaseLayout lang={lang} title="FAQ – Tonti" description="Questions fréquentes sur la cagnotte digitale Tonti.">
  <section class="py-16">
    <div class="mx-auto w-full max-w-4xl px-4">
      <h1 class="mb-10 text-3xl font-semibold text-white">Questions fréquentes</h1>
      <div class="space-y-4">
        {faqs.map((item) => (
          <div
            key={item.question}
            class="rounded-3xl border border-white/10 bg-white/[0.04] p-6 text-sm text-white/70 shadow-lg shadow-primary/5"
          >
            <h2 class="text-lg font-semibold text-white">{item.question}</h2>
            <p class="mt-2">{item.answer}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
